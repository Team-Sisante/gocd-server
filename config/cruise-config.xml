<!-- config/cruise-config.xml -->
<cruise schemaVersion="139">
  <server agentAutoRegisterKey="default" serverId="__SERVER_ID__" tokenGenerationKey="__TOKEN_GENERATION_KEY__">
    <security>
      <authConfigs>
        <authConfig id="password_file" pluginId="cd.go.authentication.passwordfile">
          <property>
            <key>PasswordFilePath</key>
            <value>/godata/config/password.properties</value>
          </property>
        </authConfig>
      </authConfigs>
      <roles>
        <role name="gocd-system-administrators">
          <users>
            <user>admin</user>
          </users>
        </role>
      </roles>
      <admins>
        <user>admin</user>
      </admins>
    </security>
    <siteUrls>
      <siteUrl>__SITE_URL__</siteUrl>
    </siteUrls>
    <artifacts>
      <artifactsDir>artifacts</artifactsDir>
    </artifacts>
  </server>

  <pipelines group="humrine_site_group">
    <pipeline name="humrine_site-artifacts">
      <materials>
        <git url="__HUMRINE_SITE_REPO_URL_WITH_CREDENTIALS__" branch="master" dest="humrine_site" />
      </materials>
      <stage name="build_and_push">
        <approval type="manual" />
        <jobs>
          <job name="artifact_generation">
            <resources>
              <resource>docker</resource>
              <resource>humrine_site</resource>
            </resources>
            <tasks>
              <exec command="bash">
                <arg>-c</arg>
                <arg><![CDATA[
                  git config --global --add safe.directory /humrine_site
                  cd /humrine_site
                  npm ls axios >/dev/null 2>&1 || npm install axios
                  export GITHUB_TOKEN='__GITHUB_TOKEN__'
                  node Scripts/build-and-push.js
                  # Record the SHA for future promotion
                  git rev-parse --short HEAD > /tmp/humrine_site-sha.txt
                  echo "ARTIFACT_SHA=$(cat /tmp/humrine_site-sha.txt)"
                ]]></arg>
              </exec>
            </tasks>
          </job>
        </jobs>
      </stage>
    </pipeline>

    <pipeline name="humrine_site-staging">
      <materials>
        <pipeline pipelineName="humrine_site-artifacts" stageName="build_and_push" />
      </materials>
      <stage name="deploy_staging">
        <approval type="manual" />
        <jobs>
          <job name="remote_deploy">
            <resources>
              <resource>docker</resource>
              <resource>humrine_site</resource>
            </resources>
            <tasks>
              <exec command="bash">
                <arg>-c</arg>
                <arg>node /gocd-server/Scripts/deploy.js humrine_site staging __GITHUB_TOKEN__</arg>
              </exec>
            </tasks>
          </job>
        </jobs>
      </stage>
    </pipeline>

    <pipeline name="humrine_site-production">
      <materials>
        <pipeline pipelineName="humrine_site-artifacts" stageName="build_and_push" />
      </materials>
      <stage name="deploy_production">
        <approval type="manual" />
        <jobs>
          <job name="remote_deploy">
            <resources>
              <resource>docker</resource>
              <resource>humrine_site</resource>
            </resources>
            <tasks>
              <exec command="bash">
                <arg>-c</arg>
                <arg>node /gocd-server/Scripts/deploy.js humrine_site production __GITHUB_TOKEN__</arg>
              </exec>
            </tasks>
          </job>
        </jobs>
      </stage>
    </pipeline>
  </pipelines>

  <pipelines group="badminton_court_group">
    <pipeline name="badminton_court-artifacts">
      <materials>
        <git url="__GIT_REPO_URL_WITH_CREDENTIALS__" branch="master" dest="badminton_court" />
      </materials>
      <stage name="build_and_push">
        <approval type="manual" />
        <jobs>
          <job name="artifact_generation">
            <resources>
              <resource>docker</resource>
              <resource>badminton_court</resource>
            </resources>
            <tasks>
              <exec command="bash">
                <arg>-c</arg>
                <arg><![CDATA[
                  git config --global --add safe.directory /badminton_court
                  cd /badminton_court
                  npm ls axios >/dev/null 2>&1 || npm install axios
                  export GITHUB_TOKEN='__GITHUB_TOKEN__'
                  node Scripts/build-and-push.js
                  # Record the SHA for future promotion
                  git rev-parse --short HEAD > /tmp/badminton_court-sha.txt
                  echo "ARTIFACT_SHA=$(cat /tmp/badminton_court-sha.txt)"
                ]]></arg>
              </exec>
            </tasks>
          </job>
        </jobs>
      </stage>
    </pipeline>

    <pipeline name="badminton_court-staging">
      <materials>
        <pipeline pipelineName="badminton_court-artifacts" stageName="build_and_push" />
      </materials>
      <stage name="deploy_staging">
        <approval type="manual" />
        <jobs>
          <job name="remote_deploy">
            <resources>
              <resource>docker</resource>
              <resource>badminton_court</resource>
            </resources>
            <tasks>
              <exec command="bash">
                <arg>-c</arg>
                <arg>node /gocd-server/Scripts/deploy.js badminton_court staging __GITHUB_TOKEN__</arg>
              </exec>
            </tasks>
          </job>
        </jobs>
      </stage>
    </pipeline>

    <pipeline name="badminton_court-production">
      <materials>
        <pipeline pipelineName="badminton_court-artifacts" stageName="build_and_push" />
      </materials>
      <stage name="deploy_production">
        <approval type="manual" />
        <jobs>
          <job name="remote_deploy">
            <resources>
              <resource>docker</resource>
              <resource>badminton_court</resource>
            </resources>
            <tasks>
              <exec command="bash">
                <arg>-c</arg>
                <arg>node /gocd-server/Scripts/deploy.js badminton_court production __GITHUB_TOKEN__</arg>
              </exec>
            </tasks>
          </job>
        </jobs>
      </stage>
    </pipeline>
  </pipelines>

  <pipelines group="pay-sol-group">
    <!-- unchanged -->
    <pipeline name="pay-sol-artifacts">
      <materials>
        <git url="__PAYSOL_REPO_URL_WITH_CREDENTIALS__" branch="master" />
      </materials>
      <stage name="build_and_push">
        <approval type="manual" />
        <jobs>
          <job name="artifact_generation">
            <resources>
              <resource>docker</resource>
              <resource>pay-sol</resource>
            </resources>
            <tasks>
              <exec command="bash">
                <arg>-c</arg>
                <arg><![CDATA[
                  export GITHUB_TOKEN='__GITHUB_TOKEN__'
                  node /pay-sol/Scripts/build-and-push.js
                ]]></arg>
              </exec>
            </tasks>
          </job>
        </jobs>
      </stage>
    </pipeline>

    <pipeline name="pay-sol-staging">
      <materials>
        <pipeline pipelineName="pay-sol-artifacts" stageName="build_and_push" />
      </materials>
      <stage name="deploy_staging">
        <approval type="manual" />
        <jobs>
          <job name="remote_deploy">
            <resources>
              <resource>docker</resource>
              <resource>pay-sol</resource>
            </resources>
            <tasks>
              <exec command="bash">
                <arg>-c</arg>
                <arg>node /pay-sol/Scripts/deploy.js staging __GITHUB_TOKEN__</arg>
              </exec>
            </tasks>
          </job>
        </jobs>
      </stage>
    </pipeline>

    <pipeline name="pay-sol-production">
      <materials>
        <pipeline pipelineName="pay-sol-artifacts" stageName="build_and_push" />
      </materials>
      <stage name="deploy_production">
        <approval type="manual" />
        <jobs>
          <job name="remote_deploy">
            <resources>
              <resource>docker</resource>
              <resource>pay-sol</resource>
            </resources>
            <tasks>
              <exec command="bash">
                <arg>-c</arg>
                <arg>node /pay-sol/Scripts/deploy.js production __GITHUB_TOKEN__</arg>
              </exec>
            </tasks>
          </job>
        </jobs>
      </stage>
    </pipeline>
  </pipelines>

</cruise>